Zeby uruchomić program, proszę wpisać:
make
./ex5ArrayBased

Operacje dominujące:

    isEmpty:
        if;
        O(1);
    
    isFull:
        if;
        O(1);
    
    getSize:
        return;
        O(1);
    
    enqueue:
        items[size + front] = element;
        O(1);
    
    dequeue:
        for;
        O(n);

    getFront:
        return;
        O(1);

    getRear:
        return;
        O(1);