Żeby uruchomić program, proszę wpisać:
make
./ex5PointerBased

Operacje dominujące:

    isEmpty:
        if;
        O(1);
    
    getSize:
        return;
        O(1);
    
    enqueue:
        while()
        O(n);
    
    dequeue:
        front = front->next;
        O(1);

    getFront:
        return;
        O(1);

    getRear:
        return;
        O(1);